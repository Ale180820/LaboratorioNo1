
namespace Laboratorio1
{
    public enum TokenType {
        Plus = '+',
        Star = '*',
        Div = '/',
        Less = '-',
        LParen = '(',
        RParen = ')',
        EOF = (char)0,
        Symbol = (char)1
    }
}
